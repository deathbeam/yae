language: java

install:
  - sudo apt-get install luajit
  - sudo apt-get install luarocks
  - sudo luarocks install moonscript

jdk: openjdk7

script:
  - javac lib/*.java
  - jar cvfe bin/non.jar Main -C lib .
  - java -jar bin/non.jar hello moon
  - java -jar bin/non.jar build desktop
