file("../../project").mkdir()

task pong << {
    copy {
        from "pong"
        into "../../project"
    }
}

task physics << {
    copy {
        from "physics"
        into "../../project"
    }
}

task require << {
    copy {
        from "require"
        into "../../project"
    }
}